# Witchaphon.github.io
let deleteButton = document.createElement("button");
      deleteButton.className = "btn btn-danger btn-sm float-end";
      deleteButton.innerHTML = "ลบ";
      deleteButton.addEventListener("click", function() {
        newElement.remove();